// Barrel file — re-exports all TypeScript types for clean imports like `from 'types'`.
export type { Button, Config, Content } from './config.interface';
export type { Theme, Themes } from './theme.interface';
